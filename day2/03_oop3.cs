using System.Drawing;
using static System.Console;

// 프로그램에서 Rect 타입을 많이 사용
//=> 직접 만들어도 되지만 기본 클래스 라이브러리(Base Class Library)에서 제공. 10_000개 이상.
class Program
{
    public static void Main()
    {
        // Rect rc = new Rect();
        Rectangle rc = rc.Rectangle(0,0,10,10); // c# 표준이 제공하는 타입

        // Console.WriteLine("면적 : {0}", area);
    }
}